# EHFNet 项目专利撰写大纲（中文）

## 0. 建议专利名称（可选）
- 一种基于 **SE(3)×T^m 流形条件流匹配** 的蛋白质-配体分子对接方法、系统、设备及存储介质。

## 1. 技术领域
- 本发明属于计算化学与人工智能交叉领域。
- 具体涉及：基于等变神经网络、异构图学习与流匹配生成模型的分子对接建模与优化。

## 2. 背景技术
- 现有分子对接方法在刚体位姿与配体柔性构象联合优化时存在：
  - 刚体与扭转自由度耦合困难；
  - 旋转等变/不变性约束不足导致泛化不稳定；
  - 多尺度（原子-残基）交互表征不充分；
  - 大规模训练中显存与 OOM 稳定性不足。
- 传统打分函数或单步回归方法难以学习从随机初态到真实结合态的连续动力学路径。

## 3. 发明目的
- 提供一种统一建模平移、旋转、扭转速度场的分子对接方法。
- 在保证几何等变/不变约束的前提下，提高对接构象生成精度与训练稳定性。
- 通过层次异构图与动态边构建增强跨尺度相互作用建模能力。

## 4. 技术方案总述（独立权利要求核心）
### 4.1 总体流程
1. 获取蛋白质与配体结构数据并编码为多类型节点特征；
2. 构建包含原子、分子、残基、口袋节点的异构图；
3. 在训练阶段，基于条件流匹配采样时间步并生成插值构象及目标速度；
4. 通过分层等变编码器提取时变几何语义；
5. 分别预测刚体平移速度、旋转速度与扭转角速度；
6. 采用切空间损失进行监督，并在推理阶段通过 ODE 积分生成最终对接姿态。

### 4.2 数学建模要点
- 状态空间为 \(\mathrm{SE}(3)\times\mathbb{T}^m\)。
- 通过条件速度场 \(v_\theta(x_t,t)\) 学习从随机初态 \(x_0\) 到目标结合态 \(x_1\) 的连续演化。
- 将原子笛卡尔速度分解为：
  - 分子级平移速度 \(v_{trans}\)；
  - 分子级旋转角速度 \(v_{rot}\)；
  - 键级扭转角速度 \(v_{torsion}\)。

## 5. 关键创新点（建议作为从属权利要求簇）
### 5.1 创新点 A：分层异构图等变编码架构
- 四阶段消息传递：层内精化、向上聚合、层间交互、向下广播。
- 原子层采用 EGNN 坐标更新，非原子层采用帧感知卷积。
- 在同一网络中统一处理局部几何细节与全局口袋语义。

### 5.2 创新点 B：Frame-aware 严格旋转不变消息
- 为目标节点构造局部正交参考帧。
- 将相对位移投影至局部帧后参与消息计算，保证全局旋转变换下消息不变。
- 可显著降低姿态变化导致的特征漂移。

### 5.3 创新点 C：平移读出的 Hybrid Fusion
- 融合“EGNN 位移质心先验分支”与“体坐标系 MLP 分支”。
- 通过门控网络自适应调节两分支权重，再乘以可学习尺度。
- 在早期/后期时间步自适应切换几何先验与语义表征贡献。

### 5.4 创新点 D：主惯量帧约束的旋转/平移等变投影
- 基于质量加权主惯量帧进行 body→world 投影。
- 旋转头直接在体帧输出轴角向量，避免量纲不匹配。
- 投影后天然满足刚体旋转等变性。

### 5.5 创新点 E：联合自由度课程学习与流匹配控制
- 平移扰动尺度、最大旋转角、扭转尺度随 epoch 渐进扩展。
- 训练中显式构造插值路径并分解目标速度，避免直接回归终态导致不稳定。

### 5.6 创新点 F：动态跨图边重建与覆盖修复
- 训练过程中动态重建 ligand-protein 原子/残基跨图边。
- 半径图为空时回退 kNN；并对未覆盖配体原子进行补边修复。
- 避免跨图信息断链，提高复杂构象下鲁棒性。

### 5.7 创新点 G：时间感知多任务损失机制
- 在切空间监督平移/旋转/扭转速度；
- 结合能损失仅在后期时间步激活；
- 位阻惩罚采用 \(t^4\) 动态加权，平滑增强后期约束。

### 5.8 创新点 H：工程稳定性机制
- 样本级梯度累积 + 梯度裁剪 + EMA；
- 基于边预算的动态批采样；
- OOM 分级恢复、级联熔断与自适应降批回升策略。

## 6. 权利要求书建议结构
### 6.1 独立权利要求 1（方法）
- 覆盖：数据获取、异构图构建、条件流匹配训练、等变速度预测、ODE 推理生成。
- 明确三类输出（平移/旋转/扭转）及其联合优化。

### 6.2 从属权利要求（建议）
- 从属 2：四阶段分层消息传递结构；
- 从属 3：帧感知卷积与局部帧构建；
- 从属 4：Hybrid Fusion 平移读出机制；
- 从属 5：主惯量帧旋转投影机制；
- 从属 6：动态跨图边 + kNN 回退 + 覆盖修复；
- 从属 7：课程学习参数调度；
- 从属 8：时间门控亲和力损失与动态位阻惩罚；
- 从属 9：OOM 熔断与自适应批预算；
- 从属 10：Euler/RK4 推理积分策略。

### 6.3 独立权利要求 2（系统/装置）
- 数据预处理模块、图构建模块、流匹配训练模块、速度场预测模块、姿态积分模块。

### 6.4 独立权利要求 3（电子设备）
- 处理器 + 存储器，执行程序以实现上述方法。

### 6.5 独立权利要求 4（存储介质）
- 存储有程序指令，执行时实现上述方法步骤。

## 7. 说明书实施方式（建议章节）
1. 数据准备与特征定义；
2. 异构图结构与边类型定义；
3. 分层编码器结构与公式；
4. 速度分解目标构建与流匹配训练；
5. 多头读出（平移/旋转/扭转/亲和力）；
6. 损失函数与训练策略；
7. 推理 ODE 采样流程；
8. 实验设置与效果对比。

## 8. 附图建议
- 图1：系统总体框图；
- 图2：异构图节点/边类型示意；
- 图3：四阶段分层消息传递结构；
- 图4：Hybrid Fusion 平移读出与主惯量帧投影；
- 图5：流匹配训练与 ODE 推理流程图；
- 图6：动态边重建与覆盖修复逻辑图。

## 9. 可量化技术效果（说明书/答审可用）
- 提升对接姿态精度（如 RMSD、中心距离等指标）；
- 提升候选构象排序相关性（Pearson/Spearman）；
- 降低训练过程异常中断率（OOM 相关指标）；
- 在不同蛋白口袋构象下保持更高泛化稳定性。

## 10. 撰写与答审注意事项（中国专利）
- 避免将“网络名称”作为唯一限定，强调“步骤+约束+模块关系”；
- 对关键术语给出明确定义（局部帧、等变投影、速度分解）；
- 在权利要求中保留参数区间/可选实现，避免过窄；
- 说明书中提供至少一个可复现实例与参数配置；
- 准备 2-3 组对比实验支撑“显著进步”。
