# Python 代码生成与重构系统指令

## 1. 适用环境与优先级
- 基础环境：Python 3.10+。
- 代码总体遵循 Google Python 代码风格；若本规则有更严格约束，以本规则为准。
- 本规则适用于新代码、修改代码和重构代码。
- 违反本规则的代码在修改时必须同步重构到符合规范。

## 2. 核心原则
- 代码必须保持统一、简洁、稳定，不保留临时性、过渡性或兼容逻辑。
- 修改代码、重构模块或实现新功能时，必须优先采用符合系统边界与最佳实践的正式实现，不得以最小改动为目标保留错误抽象、重复分支或局部补丁。
- 若旧逻辑被废弃，必须彻底删除，不得保留兼容桥、旧字段映射、回退路径或历史别名。
- 残留旧实现默认直接删除；仅在必须兼容既有模型产物、历史数据或其他已落地资产时，才允许短期保留。
- 短期保留的旧实现必须与主路径隔离，统一放入独立文件或独立模块，并以明确废弃标记限定适用边界。
- 新旧实现之间的嫁接、转换、兼容入口或适配逻辑，必须与主路径隔离，便于后续通过删除隔离文件或模块完成清理。
- 不写历史说明式注释，不描述开发过程。
- 严禁在代码中出现 TODO、FIXME、HACK。
- 若修改影响使用方式、训练流程、参数语义、目录结构、脚本行为、配置项、命令行参数或模块导入方式，必须同步更新 README。

## 3. 命名、语言与输出
- 变量、函数、类和模块名必须使用准确的英文表达语义。
- 除人尽皆知且无歧义的固定缩写外，命名一律禁止使用缩写；
- 严禁使用 legacy、compat、obsolete、tmp、v1、v2 等历史或模糊命名。
- 注释与 Docstring 默认使用简体中文。
- 专业术语、库名、类名、函数名可保留英文，但严禁中英整句混写。
- 所有输出到终端、日志或异常信息的文本默认使用英文，并在同一模块内保持一致风格。
- 中文注释使用中文标点；中文冒号后不额外添加空格。

## 4. 模块级 Docstring
- 每个 Python 文件必须包含模块级 Docstring。
- Docstring 必须使用三引号，且三引号独占一行。
- 模块 Docstring 必须概括文件职责、主要处理对象或流程以及模块边界。
- 不得使用低信息描述。

## 5. 类与函数 Docstring
- 模块、类、函数和方法必须编写 Docstring。
- Docstring 必须说明对象职责、参数语义、返回值含义以及可能抛出的异常。
- 若存在副作用、前置条件或特殊返回约定，必须明确说明。
- Docstring 结构标签统一使用 Args:、Returns:、Raises:。
- 行内注释仅用于解释复杂逻辑或关键设计原因，不解释显而易见的代码行为。

## 6. 科研与算法代码说明
对算法、训练、推理或模型代码，Docstring 必须说明：
- tensor shape
- 各维度语义（如 batch、node、edge 等）
- mask 或 index 的映射关系
- 坐标系或参考系约定
- score、energy、loss 等指标的优化方向

## 7. 类型注解与参数设计
- 必须使用 Python 3.10+ 原生类型语法，例如 list[str]、dict[str, int]、tuple[int, int]、A | B。
- 不使用 List、Dict、Tuple、Optional、Union 等旧式 typing 写法。
- 除非有明确说明，否则严禁使用 type: ignore。
- 当函数参数超过 2 个时，必须使用 * 将后续参数声明为关键字参数。
- 参数命名必须表达清晰语义。

## 8. 导入、配置与代码结构
- 删除未使用导入，避免重复导入，所有导入集中在文件顶部。
- 公共 API 必须通过 __init__.py 导出。
- 对外使用公共能力时必须优先从包入口导入，不直接依赖实现文件路径。
- 严禁将运行参数、路径、阈值等硬编码写入逻辑代码。
- 所有运行参数必须统一放在 config 或 configs 中管理。
- 函数必须保持单一职责，过长函数必须拆分。
- 模块内部代码风格必须保持一致。
