# 项目开发规范

## 1. 基础原则

- 项目以 Python 3.12 及以上版本作为唯一兼容基线，不为更低版本 Python 保留兼容写法。
- 编写代码时优先采用 Python 3.12 及以上版本推荐的现代标准写法；旧式写法只有在更清晰或确有项目原因时才保留。
- 代码风格默认遵循 Google Python Style Guide；本文件只记录项目额外约定或需要特别强调的规则。
- 如果项目配置了 `ruff`、`black`、`mypy`、`pyright` 等工具，自动化工具配置优先于文字约定。
- 不使用已废弃或已明确不推荐的语法、标准库接口和第三方 API。

## 2. 环境与依赖管理

- 默认使用 `uv` 管理虚拟环境、依赖和项目命令。
- 创建或更新环境时使用 `uv sync`。
- 添加依赖时使用 `uv add <package>`。
- 移除依赖时使用 `uv remove <package>`。
- 运行项目命令、脚本、测试、格式化或静态检查时，优先使用 `uv run <command>`。
- 不直接使用 `pip` 安装依赖，除非有明确原因。

## 3. 注释与 Docstring

- 代码中的注释和 docstring 使用英文。
- 注释应聚焦设计意图、边界条件、非显而易见的行为或重要约束。
- 模块、公共类、公共函数、公共方法的 docstring 遵循 Google 风格。
- docstring 中的参数说明顺序应与函数定义中的参数顺序一致。
- 私有辅助函数不强制写 docstring，除非逻辑复杂或容易误用。

## 4. 类型、函数接口与调用

- 项目自定义函数、方法、类属性、数据结构字段和重要变量应声明类型。
- 不允许使用 `type: ignore` 掩盖类型问题；应通过修正类型定义、接口设计、数据结构或类型收窄来解决。
- `self`、`cls` 和 `__init__` 的返回值按 Google Python Style Guide 处理。
- 项目自定义函数如果有两个及以上业务参数，必须定义为 keyword-only 参数，避免任何调用方依赖位置顺序传参。
- 调用项目自定义函数时，如果函数只有一个业务参数，可以直接传入；如果有两个及以上业务参数，必须使用关键字参数。
- 项目内部调用自定义函数时，关键字参数的书写顺序默认与函数定义顺序一致。
- 对标准库、第三方库、位置参数语义非常明确的简单调用，或不支持关键字参数的 API，可按其原生调用方式使用。

## 5. 配置、安全、日志与异常

- 配置、密钥、token、密码不得硬编码在代码中。
- 端口、路径、模型名、开关、阈值等运行环境相关或部署可变参数，应放在配置文件、环境变量或统一配置对象中，不在业务代码中写死。
- 敏感配置应通过环境变量、配置文件或密钥管理服务读取。
- 涉及日志时，应使用项目统一日志封装；如果尚不存在，应优先创建轻量封装，避免分散处理日志格式和敏感信息。
- 日志不得记录密码、token、密钥、完整认证头等敏感信息。
- 日志内容应避免暴露用户隐私数据；确需排查问题时，应优先记录脱敏后的标识或摘要信息。
- 禁止无理由使用宽泛异常捕获；不得吞掉异常或用默认值掩盖真实错误。

## 6. 包结构、导入与公共 API

- 每个 Python 包目录默认创建 `__init__.py`。
- 导入规则默认遵循 Google Python Style Guide，不强制通过包根重新导出或导入公共对象。
- 不要把内部辅助函数、临时实现细节或私有对象暴露到 `__init__.py`。
- `__init__.py` 应保持轻量，只用于必要的包初始化、`__all__` 和包元信息。
- 修改公共 API、返回结构、配置键、数据字段或持久化格式时，应明确评估兼容性影响。
- 修改功能、接口、数据结构或导入路径时，必须同步检查调用链、测试和 `__init__.py` 导出。

## 7. 历史兼容与废弃代码

- 当前代码主线应面向最新设计实现，不为旧接口或旧实现混入兼容逻辑。
- 不使用 `v2`、`new`、`old`、`legacy` 等命名在主线模块中并行保留同类函数；应优先替换原函数并同步更新调用方。
- 必须保留的旧接口、旧函数或旧数据结构，应集中放入独立的废弃兼容文件，并在模块 docstring 中说明保留原因和删除条件。
- 旧接口到新接口的转换、参数适配和数据结构适配，只能发生在废弃兼容文件中。
- 废弃兼容文件可以依赖当前主线代码；当前主线代码不得反向依赖废弃兼容文件。
- 删除废弃兼容文件不应影响当前主线代码的运行、测试和公共 API。

## 8. 设计与实现质量

- 修改代码前应先理解现有结构，优先复用已有函数、类型、配置和工具。
- 优先选择正确、清晰、可维护、可测试且符合当前架构的实现方式；最小改动只用于控制风险，不应成为牺牲设计质量的理由。
- 修改应解决问题的根本原因，而不是在调用方或表面现象处堆叠 workaround。
- 逻辑应放在正确的抽象层级，不在路由层、调用方或边界适配层堆叠本应属于核心模块的处理。
- 禁止重复造轮子；已有函数、类型、配置、日志、错误处理或工具模块能满足需求时，应优先复用。
- 核心逻辑应保持可测试，避免把业务规则隐藏在难以测试的副作用、全局状态或框架边界中。
- 函数应保持单一职责和可读长度；当函数过长、嵌套过深、分支过多、同时处理多个抽象层级或难以快速理解时，应拆分为职责清晰的小函数。
- 命名应体现具体职责，避免无明确边界的 `utils`、`helpers`、`manager`、`processor` 等泛化命名。

## 9. 文件与模块边界

- 新增代码应放在职责匹配的现有模块中；只有现有模块无法清晰承载新职责时，才新增文件。
- 不要为了局部修改把无关逻辑塞进当前文件，也不要为了方便调用创建职责模糊的公共模块。
- 单个模块应保持清晰职责；当文件同时承担多类不相关职责时，应优先拆分到更合适的模块。
- 新增抽象必须能减少真实复杂度、消除重复逻辑或明确改善模块边界。
- 新增文件或模块时，应同步考虑包级 `__init__.py` 导出、调用路径和未来删除边界。
- 移动代码时，应同步清理旧位置的导入、重复实现和失效引用。

## 10. 变更流程与验证

- 修改前应尽量确认所需信息；能通过代码、测试、配置或官方文档确认的，应先自行查证，涉及业务决策、不可验证假设或高风险行为时再向用户确认。
- 不为未明确提出的需求添加功能、兼容层、配置项或扩展点。
- 修改完成后应检查并删除被替代的无效代码、重复逻辑、不再使用的分支和临时兼容代码。
- 修改完成后应优先运行与变更相关的静态检查、格式化检查或类型检查。
- 非必要不运行示例、演示脚本或完整业务流程；需要运行时应确保它们与本次变更直接相关。
- 无法运行检查时，应说明原因。
- 提交结果前应自查是否符合本文件中的项目规范。